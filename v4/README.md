# v4 — 证据分层 + 跨源确认（多源微观结构）

**待评测**（本地已跑通，目标日 20260703）。相对 v2（0.5433 当前最佳）不再赌自指 ML（v3 教训），而是压榨 100100 积分 Tushare 账号可取的**更高精度信号**，强化可解释证据主干。

## 为什么这么做（对 v1→v3 的反思）
- Task2 占 60%，按 T+8 实盘真值算 F1。v1/v2 是纯规则先验，v3 用「v2 规则的输出」当弱标签训 ML —— **自指循环**，上限即规则本身，故 v3 反而退步（0.5311 < 0.5433）。
- 结论：与其用 ML 拟合规则，不如给规则喂**更强的原始证据**。v4 走这条路。

## v4 新接入的数据源（均 100/100 覆盖）
| 数据源 | 字段 | 用途 |
|---|---|---|
| `limit_list_d` | 连板数/涨停/炸板/封单额 | **游资最干净的铁证**（连板≈游资），比龙虎榜多覆盖 68 条/窗口 |
| `cyq_perf` | winner_rate / 主力成本 | 获利盘 + 成本偏离 → 意图与派发信号 |
| `moneyflow_dc` `moneyflow_ths` | 东财/同花顺净额、5日净额 | **三源资金流符号一致性** + 持续性 |
| `stk_limit` | 涨跌停价 | 收盘封板状态（封涨停≈买入铁证） |
| `bak_daily` | strength/activity/attack | 攻击性/活跃度行为因子 |

## 方法
- **Task2 资金类型**：硬证据分层（量化席位→连板/游资→封板抢筹）→ 跨源截面打分兜底。硬证据 8 只，软打分 92 只。
- **Task2 意图**：封板→炸板出货→三源一致（需净额达标）→单源强信号→5日持续→T0。阈值对齐 v2 保稳健。
- **Task1**：判别特征从 14 维扩到 21 维（含连板/筹码/攻击性/三源一致），8 类 greedy 唯一语义标签。

## 本地分布（20260703）
- 资金类型：散户 48 / 量化 28 / 游资 24
- 意图：T0 39 / 卖出 33 / 买入 28
- 聚类：轮廓 0.1545 / CH 18.47
- 相对 v2 改判 9 只 capital_type、11 只 intention

## 运行
```bash
.venv/bin/python shared/fetch_daily.py --start 20260608 --end <评测日> --out data/daily_hist.csv
.venv/bin/python v4/main_daily.py --target-date <评测日>
.venv/bin/python shared/make_submit.py --dir v4/out --zip v4/submit.zip
```

## 已验证
- ✅ 无未来函数：0630 特征仅依赖 ≤0630 数据（截断复算一致）
- ✅ 格式校验通过，100 行，`transaction_date=20260703`
- ✅ 早期短历史日（20260611）可鲁棒运行

> 提交后请在 [CHANGELOG.md](../CHANGELOG.md) 回填评测分。若不及 v2，则 v2 仍为基线。
